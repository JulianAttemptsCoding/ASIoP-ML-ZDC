# Raw Geant4 simulation output — too large for GitHub LFS free tier (up to 446 MB per file)
# Store locally only; regenerate from steering_*.py if needed
outfile_*.root

# Regenerable analysis archive (TBrowser QA file); rebuild with scripts/run_all.sh
qa/*.root

# ROOT ACLiC compiled-macro build artifacts
scripts/*_C.so
scripts/*_C.d
scripts/*_C_ACLiC_dict_rdict.pcm
*_ACLiC_*

# Unix NFS lock files
.nfs*

# Python cache
__pycache__/
*.pyc
