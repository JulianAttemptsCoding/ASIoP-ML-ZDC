# Traditional reconstruction (local) — what the reproduction pipeline needs
uproot>=5.0
awkward>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# ML phase 2 (Vertex `pytorch-xla` image already provides torch; for local dev:)
# torch>=2.0
# pyyaml>=6.0

# Packaging for Vertex sdist
build>=1.0
